name: 'stock_analytics'
version: '1.0.0'
config-version: 2

profile: 'stock_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# Global model configs
models:
  stock_analytics:
    staging:
      +materialized: view
      +schema: STAGING        # maps to your existing STAGING schema
    intermediate:
      +materialized: view
      +schema: INTERMEDIATE   # maps to your existing INTERMEDIATE schema
    marts:
      +materialized: table
      +schema: MARTS          # maps to your existing MARTS schema     

vars:
  # Adjust this to control how far back to load data
  start_date: '2020-01-01'